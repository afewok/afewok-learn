package leetcode

import (
	"fmt"
	"testing"
)

/**
 * 93. 复原 IP 地址
 */

func Test_leetcode_093(t *testing.T) {
	fmt.Println(climbStairs(1))
}

func restoreIpAddresses(s string) []string {
	defer timeCost()()
	// mp := make(map[int]bool)
	// for _, candidate := range candidates {
	// 	mp[candidate] = true
	// }
	// for i := 1; i < target; i++ {

	// }
	return nil
}
